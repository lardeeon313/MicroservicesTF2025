import ResetPasswordForm from "../components/ResetPasswordForm";

const ResetPasswordPage = () => {
  return (
    <main className="font-serif-georgia min-h-full">
      <ResetPasswordForm />
    </main>
  );
};

export default ResetPasswordPage;
