//Service para traer todos los pedidos. //Metodos get para traer todos los pedidos

import type { Order } from "../../../sales/types/OrderTypes";

export const GetOrders = async() : Promise<Order[]> => {
    
}